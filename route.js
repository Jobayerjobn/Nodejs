/*
import express from 'express';

const router = express.Router();

router.get('/about', () => {
    res.status(201).json({massage: 'Home route is working'})
});

export default router;

*/

import express from 'express';



